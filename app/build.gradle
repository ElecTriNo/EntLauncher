apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Soliq'
            keyPassword '223658'
            storeFile file('/Users/electrino/Desktop/Other Files/workspace-sign/Soliq/Soliq.keystore')
            storePassword '223658'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "uz.ent.launcher"
        minSdkVersion 14
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"

        ndk {
            moduleName "serial_port"
            ldLibs "log"
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:23.4.0'
}
